import { UiKit } from "./ui-kit";

export const metadata = {
  title: "UI-витрина — Turo",
  description: "Компоненты и блоки для сайта туров.",
};

export default function UiPage() {
  return <UiKit />;
}
